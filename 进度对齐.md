# 数字黄宾虹 · 进度对齐

> 2026-06-03 最终压缩版。全量工作历史见 git log。

---

## 当前并行项目

| 项目 | 截止 | 状态 |
|------|------|------|
| VAST Challenge 2026 MC1/MC2 | 2026-07-17 | 数据熟悉中 |
| 黄宾虹年谱数字人文 | — | 前端两屏完成 + RAG后端完成，交游长河待做 |

---

## 数据底座（已完成，只读）

| 文件 | 内容 | 说明 |
|------|------|------|
| `hbh_events_raw.json` | 5,282 条事件（1865–1955） | 从 637 页年谱切分 |
| `hbh_persons.json` | 302 人档案（类别/首末提及/次数/片段） | v3 终版，黄金标准覆盖 95.2% |
| `alias_map.json` | 315 标准名 + 363 别名 | 全组共享基准 |
| `hbh_interactions.json` | **6,587** 条互动边（11 种关系类型） | v3 终版 |
| `hbh_knowledge_graph.json` | 4,731 边 / 5,180 事件 / 302 人物 / 55 地点 / 555 作品 / 41 概念 / 15 组织 | 四轮迭代 F1=82.8% subset F1=94.5% |
| `hbh_source_layer.json` | 文本来源层 | 他人记述 3,406 / HBH 自撰 585 / 引用文献 480 / 编者按语 709 |
| `hbh_locations.json` | 3,669 条地点轨迹 | 游历/居住/通信分类 |
| `hbh_residences.json` | 四大居所时段 | 歙县→上海→北平→杭州 |
| `era_timeline.json` | 85 条中国近代大事（1865–1955） | 供"大时代"背景层 |
| `hbh_topics.json` / `hbh_creations.json` / `hbh_density.json` | 主题标注 / 创作活动 / 密度分析 | 规则词表驱动 |
| `life_journey.js` | 生命轨迹前端数据 | 迁徙路线 + 城市坐标 |
| `hbh_overview_data.js` | 前端地图标注数据 | |
| `hbh_custom_labels.js` | 401+ 条人工撰写小字 | 前端标注/概况 |

---

## 前端交付（HBH.html）

### 架构
- **单文件 `HBH.html`**（~320KB），自包含两屏 + 顶部自动隐藏切页导航
- **CSS 作用域化**：每页样式隔离到 `#hbh-map` / `#hbh-atlas`
- **JS 闭包化**：每页脚本惰性初始化，首次显示才执行，消除全局冲突
- **后续加页**：`build_hbh.py` 可复跑构建，加新页只需在 PAGES 列表追加一行
- **外部依赖**：`life_journey.js` / `hbh_overview_data.js` / `hbh_custom_labels.js` / `山水底图.png` / `黄宾虹小像.png` / `assets/china_1892_liumingjie_2560.jpg`

### 第一屏「生命舆图」
- 1892 古地图底图（`china_1892_liumingjie_2560.jpg`）
- 朱砂生命线沿迁徙路线生长 + 蓝线往返游历
- **变速播放**：时长按蓝线距离 + 事件数计算，静止年掠过很快，多事件年稍慢
- 右上巨年号(60px) + 阅读句(移到右侧栏顶部) + "本年记录"事件列表
- 暂停后可拖拽/缩放/点击地名查看该地全部事件
- 底部时间轴含"徽州·金华—上海—北平—杭州"居所阶段带

### 第二屏「实景山水舆图」
- 水墨底图 + 18 处名山胜水标签（按中国真实地理方位排布，引线竖直）
- 右侧按题材分类陈列 212 件作品缩略图
- 点击地名 → 弹出该地存世画作卡片，点击单幅放大
- 90 件实景山水标星
- **待加**：导览动线（游踪串联画风演进 + 底部墨色光谱条），见 `数字黄宾虹_后续幕次设计.md`

### 悬浮「晚年宾翁」(全局，跨屏)
- 像素小人形象（`黄宾虹小像.png`），可拖动到任意位置
- 点击 → 弹出自适应定位对话框，发消息 → **GraphRAG 后端回答**
- 答复含 `[[人物:傅雷]]`/`[[地名:歙县]]` 可点击标签（人物预留高亮钩子，地名点即跳生命舆图）
- 会话内多轮记忆（刷新清空）
- 开场白只发一次（多次开关不重复）

---

## RAG 后端（`graphrag/` + `graphrag_server.py`）

### 架构
- **实体链接**（alias_map 315 标准名）→ 知识图谱子图三元组
- **混合检索**：bge-small-zh-v1.5 稠密 + BM25(jieba) 词法 → RRF 融合
- **图→文锚定**：KG 命中边的 event_id 强制注入对应年谱切片
- **提示词组装**：填入用户给定的学术书童 System Prompt（含 graph_context + text_context + 可标记实体 + 时代背景）
- **大模型**：DeepSeek flash（Anthropic 兼容端点），key 存本地 `deepseek_key.txt`（每请求重读，不改文件即换 key），gitignore
- **重排**：bge-reranker-v2-m3 默认关闭（CPU 慢），RRF 质量已好
- **预构建索引**：`graphrag/index/`（dense.npy + bm25.pkl + chunks.jsonl），开箱即用

### 启动
```bash
python graphrag_server.py          # http://localhost:8765
python graphrag_qa.py "提问"       # 命令行直接用
```

### 关键设计决定
- 谱主不作为三元组焦点（否则灌入一生 2343 条边淹没所问对象）
- 三元组分三层排序：牵涉所问实体 > 关系类型（社交>社团>画学>游历>创作）> 年份接近度
- 虚岁 = year−1865+2（卒年 1955 = 享寿 92 岁，与年谱一致）
- 恒返回 Top-K 切片（即便无关问题），graph_context 为空时叠加诚实原则驱模型拒答
- deepseek flash 是推理模型，应答含 thinking 阶段，首问约 15-30s

### 已推 GitHub
**https://github.com/Wang-auspicious/digital-humanities-Huangbinhong**
分支 main，提交 `f799d2c`（最小可运行版，无画作图，约 29MB）。
**此版本不含本次 HBH.html 修改**（第一屏变速+布局调整、小像边缘修复、宾翁改名+开场白修复）。

---

## 当前未提交本地改动

| 文件 | 改动 |
|------|------|
| `HBH.html` | 第一屏变速(按内容算时长)+布局(阅读移到右侧/年号缩小/p隐藏)；小像边缘硬化；宾翁改名+开场白一次性+loading渲染修复 |
| `黄宾虹小像.png` | 边缘硬化（阈值160+腐蚀1px，去光环不伤头发，自动裁紧）|

---

## 待做（按优先级）

| # | 内容 | 参考文档 |
|---|------|---------|
| 1 | 第二屏「导览动线」+ 墨色光谱条 | `数字黄宾虹_后续幕次设计.md` §一 |
| 2 | **第三屏「交游长河」**（主）+ 放射星系（下钻） | `交游长河_实现草案.md` |
| 3 | 第四屏「笔墨画法」五笔七墨拆解 | `数字黄宾虹_后续幕次设计.md` §三 |
| 4 | 「大时代」作为各屏可开关背景图层 | 同上 |
| 5 | 序幕 hero（5-10s 题眼开场） | 同上 |
| 6 | 尾声「浑厚华滋」结语 + 书童收束 | 同上 |
| 7 | 「数据与方法」附录（评审/答辩用） | 同上 |
| 8 | 书童 `[[人物:]]` 标签 → 社交网络/地图跳转高亮 | 已留钩子 `HBH.onMention` |
| 9 | 本次 HBH.html 改动推 GitHub | |
